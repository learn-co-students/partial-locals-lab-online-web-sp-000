<%= form_tag "/students/search" do %>
    Search: <%= text_field_tag "search", @search %>
    <%= submit_tag "Search" %>
<% end %>

<% @students.each do |student| %>
    <%= render partial: 'student', locals: { student: student } %>
<% end %>