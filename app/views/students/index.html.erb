<%= form_tag(students_path, method: "get") do %>
  <%= label_tag(:q, "Search for:") %>
  <%= text_field_tag(:q) %>
  <%= submit_tag("Search") %>
<% end %>

<% @students.each do |student_info| %>
  <%= render partial: 'student', locals: {student: student_info} %>
<% end %>
