Classroom Info
<%= render partial: 'classroom', locals: {classroom: @classroom} %>

We now want to list all the students in the class:

<%= render partial: 'students/student', collection: @classroom.students %>
