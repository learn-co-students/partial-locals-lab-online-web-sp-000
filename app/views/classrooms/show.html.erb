Classroom Info


<%= render partial: 'classroom', locals: {classroom: @classroom} %>

We now want to call out the oldest student in the class:
<ul>
<% @classroom.students.each do |std| %>
  <li><%= render partial: 'students/student', locals: {student: std} %></li>
<% end %>
</ul>
